#Este es el archivo de configuracion
server:
  port: 8083
spring:
  profiles:
    active: dev
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    spring:
      application:
        name: servicio_progreso